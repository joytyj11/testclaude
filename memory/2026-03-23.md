# 2026-03-23 - 工作日记录

## 晚上 19:29
- 用户要求配置 GitHub 集成
- 检查发现：gh CLI 已安装 (v3.4.1)，但未登录认证
- 需要用户手动运行 `gh auth login` 完成 OAuth 认证

## GitHub 集成配置步骤
1. 运行 `gh auth login` 选择认证方式（推荐 GitHub.com 和 HTTPS）
2. 选择认证方式：Login with a web browser（最方便）
3. 浏览器打开设备码页面完成授权
4. 验证：`gh auth status`
5. 之后可以在 OpenClaw 中使用 github 技能

## 注意事项
- GitHub CLI 需要认证才能操作私有仓库和 API
- 认证后可以查看 issues, PRs, CI runs 等
- 如需自动化操作，可考虑配置 Personal Access Token

## 待办
- [ ] 等待用户完成 gh auth login
- [ ] 验证后测试 github 技能是否正常工作
